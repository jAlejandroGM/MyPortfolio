---
import { Image } from "astro:assets";
import ExternalLink from "../../icons/ExternalLink.astro";

const { image, title, link } = Astro.props;
---

{
  link ? (
    <a
      href={link}
      class="relative group block w-full"
      target="_blank"
      rel="noopener noreferrer"
    >
      {image ? (
        <img
          class="h-44 md:h-52 w-full object-cover object-center rounded-t-xl"
          src={image}
          alt={title}
        />
      ) : (
        <div
          class="h-44 md:h-52 w-full rounded-t-xl bg-deep/40"
          aria-hidden="true"
        />
      )}
      <span class="absolute rounded-t-xl inset-0 flex items-center justify-center group-hover:bg-black/60 opacity-70 lg:opacity-50 group-hover:opacity-100 transition-opacity duration-300">
        <ExternalLink class="size-8 text-primary" />
      </span>
    </a>
  ) : image ? (
    <img
      class="h-44 md:h-52 w-full object-cover object-center rounded-t-xl"
      src={image}
      alt={title}
    />
  ) : (
    <div
      class="h-44 md:h-52 w-full rounded-t-xl bg-deep/40"
      aria-hidden="true"
    />
  )
}
